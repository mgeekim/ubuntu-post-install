[user]
    name = mg0721
    email = 64mingn@gmail.com
[color]
    # Automatically colors its output.
    ui = auto
[core]
    # Ignore file permission change.
    fileMode = false
    # Set LF as default and CRLF will be changed to LF.
    # Use 'true' for windows.
    autocrlf = input
    whitespaces = cr-at-eol
    pager = less -F -X
[push]
    # Push the current branch with the same name on the remote.
    # Use 'matching' if want to push all name-matched branches
    default = simple
[http]
    sslverify = false